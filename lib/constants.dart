/// Global UI constants
library constants;

/// Opacity for the tiled background pattern.
/// 0.0 = fully transparent · 1.0 = fully opaque
const double kBackgroundOpacity = 0.03;

// delay for the splash screen number
const Duration kSplashDelay = Duration(seconds: 0);